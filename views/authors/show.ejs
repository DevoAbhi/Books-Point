<h2 class="page-header"><%= author.name %> </h2>
<div class="btn-row">
    <button class="btn btn-primary"><a href="/authors/<%= author.id %>/edit ">Edit</a></button>
<%- include('../partials/deleteForm',
                {url : `/authors/${author.id}`}) %> 
</div>



<% if(booksByAuthor.length >0) {%> 
    <h2 class="page-header">Books By Author</h2>
    <%- include('../partials/book_grid', {
        books : booksByAuthor
    }) %>    
    <% } else {%> 
        <h2>No Book has been issued by <%= author.name %> </h2>
    <% }  %> 